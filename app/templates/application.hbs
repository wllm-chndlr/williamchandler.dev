{{!-- <div class="container">
    <div class="menu">
        {{#link-to "index"}}
        <h1>
            William Chandler
        </h1>
        {{/link-to}}
        <div class="links">
            {{#link-to "index"}}
                home
            {{/link-to}}
            {{#link-to "projects"}}
                projects
            {{/link-to}}
        </div>
    </div>
    <div class="body">
        {{outlet}}
    </div>
</div> --}}


{{outlet}}

